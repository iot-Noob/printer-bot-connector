# Core dependencies
rocketchat_API>=3.5.0
rocketchat_async>=0.1.0
websockets>=12.0
requests>=2.31.0

# Configuration and validation
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# Printing
universal-printer>=3.0.0

# PDF handling (optional - for page count)
PyPDF2>=3.0.0

# Windows printer enumeration (optional)
pywin32>=306; sys_platform == 'win32'

# SSL support (already in Python standard library)
# cryptography - included in Python 3.10+

# Async support (already in Python standard library)
# asyncio - included in Python 3.10+